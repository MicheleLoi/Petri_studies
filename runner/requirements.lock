# Phase A stub — Petri version pin pending (see plan §8 gating item).
# To be regenerated via `pip-compile` (or `uv lock`) in Phase A2 once
# the Petri version is chosen and tested.

# Phase A skeleton dependencies (sufficient for runner stub + tests):
jinja2>=3.1
jsonschema>=4.20
pyyaml>=6.0
pytest>=7.4

# Phase A2 additions (commented out until version pin):
# petri==X.Y.Z         # Anthropic Petri framework — pin from latest stable
# inspect-ai==X.Y.Z    # Inspect-AI harness wrapping Petri
# anthropic==X.Y.Z     # Anthropic SDK for direct model calls
