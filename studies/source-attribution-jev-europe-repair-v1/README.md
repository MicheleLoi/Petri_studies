# Jev European supplement: prospective acquisition repair

This package amends the interrupted [Jev European supplement](https://github.com/MicheleLoi/Petri_studies/releases/tag/preregistered-source-attribution-jev-europe-descriptive-v1). The original registration, files and observations remain preserved. Read [the amendment](AMENDMENT.md) for the author's adopted policy and its limitations, and [the implementation guide](README_IMPLEMENTATION.md) for execution and reporting.

The original 288 slots, exact arguments, organizations, five-anchor rubric, payloads, block assignments and descriptive contrasts are unchanged. Two scores were already obtained; 55 service-capacity errors exhausted eighteen slots. The author authorized three additional attempts for those eighteen named slots, while untouched slots retain three total attempts. Any new HTTP 429 stops the whole collector and creates a persistent hold. No automatic release is authorized.

`provenance/` contains the original scientific plan and manifest plus hashes of the 57 preserved request/response/result triples and the original run markers. Actual scientific responses and ratings remain private. The repair must be deposited publicly and verified before any new attempt; the genuine amendment receipt links both registrations. Historical requests are checked against the original publication and new requests against the amendment publication. A GitHub release supplies a dated public deposit, without a claim of immutable third-party notarization.

`code/` implements immutable prefix checks, slot-specific attempt limits, saved-response recovery, a global 429 hold and the same descriptive report. `verification/` contains artificial fixtures and offline checks, not empirical results. `ENVIRONMENT.json` records the tested dependencies. `MANIFEST.json` gives SHA256 of exact file bytes, excluding itself. Structured payload hashes use UTF-8 JSON, sorted keys, compact separators, unescaped Unicode and no non-finite numbers. These integrity checks do not require Giano implementation code.

Jev is still addressed as `typesafe-ai/jev`; the deployed snapshot remains presumed from dated provider documentation. Its native score divided by four is not a calibration to the conversational models' ratings. No provider substitution, anonymous baseline, p-value, confidence interval or equivalence verdict is added. The report must disclose the original interruption and selective recovery. This is one repaired collection, not an independent replication.

Do not modify this package after freezing. New raw data, reports, conversations and private session identifiers are excluded from public deposit. The cumulative EUR 100 advance-notice threshold includes uncertain costs and the next-batch reserve.
