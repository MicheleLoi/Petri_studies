# Jev Europe: prospective slower acquisition

The author instructed: “try with slower paced retries”. The assistant implements a minimum **60 seconds after each transport call ends**, before preparing the next call, including retries. One worker is retained. This interval is an operational choice, not a documented provider limit. The service reported upstream high demand; no numerical request limit or Retry-After value was established.

This addendum changes the acquisition timing of the frozen European repair, whose manifest is `e5fdf0cf9147c29ef72776ece86a07abb3a516bf9f8e5686ba2f25e008b6a3a4`. It supersedes the earlier no-deliberate-pause schedule for subsequent attempts. It does not change the 288 slots, arguments, sources, rubric, payloads, order, first-valid-score selection, attempt caps, or descriptive analysis. The 61 prior attempts are preserved by hashes; four already usable slots will not be sent again. No source effects have been examined during this incomplete collection. Responses and scores remain private.

The wrapper imports the original frozen repair engine and adapter. Pacing happens before the engine reserves a request. A monotonic clock enforces the interval within a process; on resume the most recent persisted result supplies the initial wait. The original operating-system lock, lineage and budget gates remain active. Every new HTTP 429 still creates a persistent global stop: this addendum does not automatically release future holds. The author's current instruction authorizes release of the existing hold only, through the existing dated authorization mechanism. No attempt limits are expanded.

Publish this package and verify its public release before execution. The wrapper verifies its own manifest, public-deposit receipt, base manifest, and preserved prefix. Invoke `paced_runner.py` with `--base-package`, `--study-root`, `--launch-record` (the genuine repair receipt), `--pacing-record` (the genuine public receipt for this addendum), and `--resume-authorization` (the current private dated instruction). Paths and credentials are supplied privately. An ordinary GitHub release documents public deposition, not immutable external notarization.

The slower phase extends the observation period. Final reporting must separate the original phase, repair/resumes, and this paced phase, verify all three publication chronologies and actual pacing, and disclose the interruption and selective recovery. It is one interrupted collection, not an independent replication. The original descriptive report and contrasts remain in force; no new p-values, confidence intervals, or equivalence verdicts are added. The operational audit must additionally check each request in this phase against this addendum's publication time and the 60-second spacing. Any midnight resumption requires a fresh dated documentary identity check as before.

Offline timing tests: `python -B -m unittest test_pacing.py`. The frozen repair engine's regression tests must also pass before launch.
