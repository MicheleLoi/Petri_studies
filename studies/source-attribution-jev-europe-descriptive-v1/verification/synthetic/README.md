# Synthetic offline rehearsal

These 288 responses are generated fixtures, not API observations. All real HTTP was forbidden. Scores encode four synthetic interactions of 0.2 and one of zero as a correctness check. The report, complete structured summary, illustration selection, ratings plot and chronology plot were generated using the proposed reporting code. They are excluded from scientific data. Private filesystem paths, credentials and session metadata are absent.
